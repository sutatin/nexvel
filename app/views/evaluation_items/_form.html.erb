<%= form_for(@evaluation_item) do |f| %>
  <% if @evaluation_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@evaluation_item.errors.count, "error") %> prohibited this evaluation_item from being saved:</h2>

      <ul>
      <% @evaluation_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
